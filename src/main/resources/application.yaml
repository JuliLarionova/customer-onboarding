spring:
  application:
    name: customer-onboarding

  datasource:
    url: jdbc:postgresql://localhost:5432/customer_onboarding
    username: customer_onboarding_app
    password: localdev

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

server:
  port: 8080

logging:
  level:
    com.example.randombank: DEBUG

app:
  registration:
    allowed-countries:
      - NL
      - BE
